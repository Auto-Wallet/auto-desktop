{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "dapp",
  "description": "Untrusted remote dApp tab webviews (one per open tab, labeled dapp-<id>). Granted only narrow app-defined commands: wallet_request, http(s)-validated open_external_url, and display-only dapp_dialog. No core:*/fs:*/shell:* and no opener:* (the page never reaches the opener plugin directly). This is the wallet's security boundary.",
  "webviews": ["dapp-*"],
  "remote": {
    "urls": ["http://*:*", "https://*:*"]
  },
  "permissions": ["allow-wallet-request", "allow-open-external-url", "allow-dapp-dialog"]
}
