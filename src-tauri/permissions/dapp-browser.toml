# Permissions for the embedded-dApp-browser controls. Granted ONLY to the
# trusted "shell" webview (capabilities/default.json) — never to the dapp webview,
# so an untrusted page can never navigate or reposition any webview itself.
[[permission]]
identifier = "allow-open-dapp"
description = "Allow the shell to navigate + show the dapp webview at a content rect."
commands.allow = ["open_dapp"]

[[permission]]
identifier = "allow-set-dapp-bounds"
description = "Allow the shell to reposition/resize a dapp tab webview on layout changes."
commands.allow = ["set_dapp_bounds"]

[[permission]]
identifier = "allow-sync-toast-overlay"
description = "Allow the shell to position the local toast overlay above dapp webviews."
commands.allow = ["sync_toast_overlay"]

[[permission]]
identifier = "allow-sync-menu-overlay"
description = "Allow the shell to show the local dropdown-menu overlay above dapp webviews."
commands.allow = ["sync_menu_overlay"]

[[permission]]
identifier = "allow-reload-dapp"
description = "Allow the shell to reload a dapp tab webview."
commands.allow = ["reload_dapp"]

[[permission]]
identifier = "allow-hide-dapp"
description = "Allow the shell to hide a dapp tab webview when switching tabs/pages."
commands.allow = ["hide_dapp"]

[[permission]]
identifier = "allow-close-dapp"
description = "Allow the shell to close (destroy) a dapp tab webview when its tab is closed."
commands.allow = ["close_dapp"]

[[permission]]
identifier = "allow-set-active-chain"
description = "Allow the shell to set the active wallet network and push chainChanged to dApps."
commands.allow = ["set_active_chain"]

[[permission]]
identifier = "allow-get-active-chain"
description = "Allow the shell to read the active wallet network for the chain selector."
commands.allow = ["get_active_chain"]

[[permission]]
identifier = "allow-get-close-behavior"
description = "Allow the shell to read the main-window close-button behavior."
commands.allow = ["get_close_behavior"]

[[permission]]
identifier = "allow-set-close-behavior"
description = "Allow the shell to set the main-window close-button behavior."
commands.allow = ["set_close_behavior"]

[[permission]]
identifier = "allow-get-chains"
description = "Allow the shell to read the full chain registry (Settings network list + selectors)."
commands.allow = ["get_chains"]

[[permission]]
identifier = "allow-add-chain"
description = "Allow the shell to add a custom network."
commands.allow = ["add_chain"]

[[permission]]
identifier = "allow-update-chain"
description = "Allow the shell to edit an existing network's parameters."
commands.allow = ["update_chain"]

[[permission]]
identifier = "allow-remove-chain"
description = "Allow the shell to remove a user-added network."
commands.allow = ["remove_chain"]

[[permission]]
identifier = "allow-get-activity"
description = "Allow the shell to read persisted wallet transaction activity."
commands.allow = ["get_activity"]

[[permission]]
identifier = "allow-check-for-update"
description = "Allow the shell to manually check GitHub Releases for an app update."
commands.allow = ["check_for_update"]
