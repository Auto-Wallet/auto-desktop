/// <reference types="vite/client" />

declare const __APP_VERSION__: string;
/** PublicNode RPC API key — injected by vite under `vite dev` only, "" in builds. */
declare const __PUBLIC_NODE_KEY__: string;
